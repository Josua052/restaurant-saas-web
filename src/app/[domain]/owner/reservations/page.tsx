import ReservationsClient from "./ReservationsClient"

export default function ReservationsPage() {
  // In a real application, you might fetch initial reservations data here
  // and pass it down to ReservationsClient as props.
  // For this UI implementation, the mock data and state is handled inside the client.
  return <ReservationsClient />
}
